{% set name = "libfuse" %}
{% set version = "3.9.2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/{{ name }}/{{ name }}/releases/download/fuse-{{ version }}/fuse-{{ version }}.tar.xz
  sha256: 6999b6d48e7c0a79628fa901f6e66def3513cab4ffdd8097821e7dc3cdeae08a

build:
  number: 0
  skip: true  # [not linux]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - meson
    - ninja
    - pkg-config
  host:
    - libiconv
  run:
    - libiconv

test:
  commands:
    - test -f ${PREFIX}/lib/libfuse3${SHLIB_EXT}
    - test -f ${PREFIX}/include/fuse3/fuse.h

about:
  home: https://github.com/libfuse/libfuse
  license: LGPL-2.1
  license_file: LICENSE
  license_family: LGPL
  summary: The reference implementation of the Linux FUSE (Filesystem in Userspace) interface

extra:
  recipe-maintainers:
    - asafkahlon
